@model MvcApplicationTask.Models.MsgModels

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>MsgModels</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Date)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Date)
            @Html.ValidationMessageFor(model => model.Date)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Emiteur)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Emiteur)
            @Html.ValidationMessageFor(model => model.Emiteur)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Contenu)
        </div>
        <div class="editor-field">
            @Html.TextAreaFor(model => model.Contenu)
            @Html.ValidationMessageFor(model => model.Contenu)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
